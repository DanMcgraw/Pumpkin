# Repository Agent Instructions

## Commit completed changes

When a prompt causes files in this repository to be created, modified, renamed, or deleted, commit the completed changes at the end of prompt processing after running the appropriate validation. Use a concise commit message that describes the completed work.

Do not commit unrelated pre-existing user changes. If validation fails or the requested work is incomplete, report the problem instead of committing a knowingly broken result.
